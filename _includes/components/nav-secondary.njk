<nav>
  <ul>
  {%- for nav-secondary in collections.nav-secondary | sort(attribute="date") -%}
    <li>
      <a href="{{ nav.url | url }}"{% if nav-secondary.url == page.url %} data-current="current item"{% endif %}>{{ nav-secondary.data.navtitle }}</a>
    </li>
  {%- endfor -%}
  </ul>
</nav>
